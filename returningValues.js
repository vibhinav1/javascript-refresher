// returning values
// const speak = function(name 'luigi', time = 'night'){
// console.log( good ${time} ${name}`);
// };
const calcArea = function(radius){            //created the function
    let area = 3.14 * radius**2;
    return area;
  }
  const answer = calcArea(5);            //called the function by storing it into a variable.
  console.log(answer);
  
